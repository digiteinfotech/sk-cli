---
name: build
description: Build the sk-cli project with tsup
allowed-tools: Bash
user-invocable: true
---

Build the sk-cli project:

1. Run `npm run build` in the project root
2. Report whether the build succeeded or failed
3. If there are TypeScript errors, list them clearly
4. If build succeeds, confirm the output files in `dist/`
